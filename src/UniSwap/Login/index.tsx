import { Route, Routes, Link } from "react-router-dom";
import "../ColorScheme.css";

function Homepage() {
    return (
        <div className="pinkBackgroundColor">
            Login
        </div>
    );
}

export default Homepage;